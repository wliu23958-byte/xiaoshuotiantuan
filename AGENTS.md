# AGENTS

给人与 Agent 共用的工作方式入口。设定、情节与命名以 `01-设定/`、`02-大纲/` 的权威文档为准；本文件只规定**怎么开工、怎么改、何时用 skill**，不重述设定内容。

角色分工见 [团队手册](./团队手册.md)，通道与调度见 `.cursor/rules/pchat-session.mdc`，仓库层面的操作约定见 [agents/](./agents/README.md)。

## 开工顺序

写任何一章，按序加载材料；每步完成后再进入下一步。

1. **定章** — 从总纲的[单元排布总表](./02-大纲/总纲.md)确认本章属于哪一卷、哪个单元、主线还是支线。
   **完成**：能说出章号、单元类型、本单元对应的器与姓。
2. **定事实** — 读 [时间线台账](./01-设定/时间线台账.md) 与 [名词表](./01-设定/名词表.md)，确认本章涉及的年份、年龄、沾锈刻度、专有名词写法。
   **完成**：本章要用到的每个数字和名字都在台账里查得到。
3. **定本章** — 读 `02-大纲/章纲/第N卷章纲.md` 里对应那一条，抄下**本章新增信息**和**本章不许提前泄露的信息**。
   **完成**：两份清单都在手上；不许泄露的那份尤其要看，它挡的是提前泄底。
4. **定前情** — 读前一章正文，接住上一章的钩子与情绪落点。
   **完成**：说得出上一章结尾停在哪、本章第一段怎么接。
5. **再动笔** — 按 [写作规范](./00-调研/写作规范.md) 的尺子写：冲突落在开头那一段内，章末钩子独立成段。
   **300 字那条硬线只管首章**，规范表里写的就是「首章出冲突」；这里不复制它的数，要查具体阈值去规范表。

**第 2 步不许跳。** 第一卷初稿全线崩过一次，起因就是有人凭记忆写年份——「二十年前」和「七年前」两套说法并存，按两条线同时算，沈砚在爷爷死的时候只有六岁，而他七岁才落水。整条主线的根基就塌了。

审校、改设定、改章纲的流程各不相同，见 [agents/workflow.md](./agents/workflow.md)。

## 红线

| 目标 | 门禁 |
| --- | --- |
| 不写犯罪教程 | 不写具体盗掘手法与文物交易渠道细节 |
| 灵异有边界 | 一律限定在「器物残留信息」框架内，不写鬼神施法 |
| 不碰敏感 | 不写政治敏感与过度血腥 |
| **不靠变强供能** | 沾锈单调上升、记忆损失不可逆、沈砚只能越来越弱。爽点唯一合法来源是「规则解读 → 漏洞挖掘 → 执行反制」 |
| 专业细节不许编 | 工序、病害名称、材料、力学常识必须真实；器物、墓葬、人物可以虚构 |
| 单一权威 | 一个设定一处定义。章纲与审校只引用，不复制第二套口径 |
| 正文零 markdown | 加粗、斜体、引用块、分隔线、小标题、列表、反引号一个都不许有。**场景分隔符也不许有**（2026-08-15 改，原写「用独占一行的 `※　※　※`」）——换场直接另起一段，靠第一句落时间与地点 |

第四条是《捞尸人》用五百万字换来的教训，也是唯一一条**声明拦不住**的——滑坡不是从「决定写升级流」开始的，是从「这一卷没什么可揭的了」开始的。它的实际仪表是 [谜团台账](./02-大纲/谜团台账.md)：每个单元收束时对一次账，未解存量跌破 8 就停下来补投。

冲突时：**正文已写的优先不动，设定和大纲让步。** 只有正文自相矛盾时才动正文，且挑改动面最小的那一处。裁定细则见 [agents/domain.md](./agents/domain.md#冲突按什么顺序裁)。

## 引用另一个文件

**长期文档之间互相引用，用引文或小节标题，不用行号。** 行号失效时仍然指得很稳，只是指错了，而且没有任何东西会报——机检读的是台账里的表格，不读引用是否还成立。引文至少失配可见。

**审校报告是例外，必须带 `文件:行号` 和原文。** 它是对某一时刻某一稿的断言，不是长期引用，行号在那一刻是准的，原文让它在失配时可查。真正的规矩不是「行号好不好」，是**报错之前必须打开源文件确认原文**：凭印象报错比漏报更糟，它会让人去修一个本来没错的地方。

## 改文档

| 改什么 | 怎么走 |
| --- | --- |
| 一条已被引用的设定 | 先在黑板提 `kind=decision`，总编确认后再改，并通知已引用的成员。流程见 [agents/domain.md](./agents/domain.md#改设定要走的流程) |
| 名词表、时间线台账 | 只加自己那一行，不动邻行。格式是机器可解析的，列顺序不能变 |
| 章纲 | 改完跑 `check-outline.mjs` 与 `check-density.mjs` |
| 正文 | 改完跑 `check-format.mjs` 与 `check-facts.mjs`，两个都退 0 才算完 |
| 新立任何设定 | `post_to_blackboard`（`kind=contract`）锁定，并通知设定师登记进名词表 |

**正文里出现的任何量化值——百分比、秒数、毫米、次数——都要回写台账。** 一个只存在于正文对话里的数字等于没有定义，下一个写手会自己编一个新的出来。

**不要用 `Set-Content` 改中文文件**，它会写成乱码或带 BOM；原因与替代写法见 [agents/workflow.md 的本机环境](./agents/workflow.md#本机环境)。

## Skills 路由

| 意图 | Skill |
| --- | --- |
| 按章纲写一章正文 | `/novel-chapter-draft` |
| 审一章初稿，往死里挑 | `/novel-chapter-grill` |
| 查正文排版有没有 markdown 装饰 | `novel-chapter-format`（自动拾取） |
| 查正文与台账对不对得上 | `novel-fact-check`（自动拾取） |

前两个声明了 `disable-model-invocation: true`，**只在显式打 `/name` 时才跑**，不会被自动拾取，因此也不出现在可用技能列表里。看不见不等于没装。

后两个各自还带若干脚本（章纲体检、密度体检、重复数字、家底盘点、全书台账），用法见各自的 `SKILL.md`。一次跑全套：

```powershell
[Console]::OutputEncoding=[Text.Encoding]::UTF8
npm run check
```

**改了 skill 要新开会话才加载**，当前会话的技能列表是会话开始时的快照。

### Skill 可见执行

Skill 是 Agent 作业流程，不是 shell 命令，Cursor 不一定显示 skill 名。用户显式触发 skill 时，让人能盯到进度：

1. **开场** — 首条回复写明 `按 /skill-name · Step N：…`，并一句点出本 skill 目的
2. **换步** — 每次进入下一步单独起一行，完成标准用半句标出，与 skill 正文一致，不另造阶段
3. **等人** — 需要拍板时写 `⏸ 等待你决定`，收到答复后写 `▶ 继续 Step N`
4. **收工** — 写 `✓ /skill-name 完成` 并列产物路径；中途收口写 `◼ /skill-name 暂停于 Step N` 与恢复条件

禁止只默默读写文件却不点名 skill。

## agents/

仓库层面的操作约定，五份：

| 文件 | 管什么 |
| --- | --- |
| [domain.md](./agents/domain.md) | 设定真源、目录归属、冲突裁定顺序、改设定的流程 |
| [workflow.md](./agents/workflow.md) | 一章从认领到入库的流程、机检、红线生效点、本机环境 |
| [issue-tracker.md](./agents/issue-tracker.md) | 问题往哪登记：审校记录 / 黑板 / PChat 任务 |
| [triage-labels.md](./agents/triage-labels.md) | 严重度、归属、任务状态、脚本退出码的口径 |
| [parallel-sessions.md](./agents/parallel-sessions.md) | 多个窗口共写一棵树时的地盘、暂存、报告 |

## 当前状态

**这里不写章数和字数。** 写死的进度数字每写一章就过期一次，而它旁边的结论会跟着一起烂掉。

要看家底就跑一次：

```powershell
[Console]::OutputEncoding=[Text.Encoding]::UTF8
npm run inventory
```

它两边都扫——新位置 `02-大纲/章纲/` 和旧位置 `02-大纲/第N卷细纲.md`。`check-outline.mjs` 只认新位置，所以两者的差额就是它看不见的部分。**卷一那 30 条在两个位置各有一份，是重复不是缺口**，改章纲时记得另一份也在。**卷五尾声那 2 条（第 499、500 章）同理**——细纲的尾声按章立了标题，盘点会把它计成旧位置 2 条。不要去清理它：第五卷章纲开头写明「本文件与细纲不一致时以细纲为准」，细纲是卷五的上游，而且多份审校记录与裁决记录按行号引着它，增删它的行会把那些引用全部指偏。正文进度直接看 `03-正文/` 的最大章号。

# 会话启动词

开一个新窗口加入协作时，把下面这段填好发给它。五个窗口各发一次。

## 模板

```
你是 idp-mcp-<N>，与其他 <M> 个会话共写同一棵工作树 D:\VScodeSpace\xiaoshuotiantuan。
这是长篇小说《合缝》的写作仓，先读 AGENTS.md 与 agents/parallel-sessions.md，按其中做法协同。

你的角色：<设定师 / 大纲师 / 主笔 / 审校>
地盘：你只写 <目录或文件>，不碰其他路径；要越界先在频道里说，对方让位再动。
本轮任务：<具体做什么>

然后调用 idp-mcp-<N> 的 check_messages 进入监听。
```

## 填的时候注意

**`<N>` 是通道号，不是角色编号。** 每个 Cursor 窗口绑一个 `idp-mcp-N`，窗口和角色的对应关系每次现分，不要写死在任何文件里。

**地盘按目录填，一个目录一个写入方**（归属表在 [agents/domain.md](./agents/domain.md#目录归属)）：

| 角色 | 地盘 |
| --- | --- |
| 设定师 | `01-设定/` |
| 大纲师 | `02-大纲/` |
| 主笔 | `03-正文/`，且**填到具体章节文件**，不是整个目录 |
| 审校 | `04-审校/` |
| 总编 | `00-调研/`，以及派活与验收 |

主笔那一栏要写成 `03-正文/第021章-*.md` 这种粒度。两个主笔同时开工时，写整个目录等于没分地盘。

**「本轮任务」要能一轮做完。** 任务超出一个工作窗口就会被系统催、然后转给别人，而**每次转手都会把已经做的部分丢掉**。一章正文、一卷章纲的一个区段、一轮审校，都是合适的粒度；「把卷三写完」不是。

**只读会话也要派具体任务。** 没有派活的空闲窗口是负收益——它们会互相仲裁同一件事。好任务的形状是「只读、边界清晰、结论能直接改变写入方的下一步」，并且要写明**不要做什么**（不改文件、不替别人重写、不下设定裁定），否则调研会变成第二份实现。

## 总编那一个不一样

总指挥窗口不发上面这段。它的开场是认领导航：`list_sessions` 看谁在线，`set_member_tags` / `set_member_role` 给其余成员打标签，然后 `set_work_group_goal` 定阶段目标。

派活选哪个原语看形状——可并行的批量用 `push_backlog`，有依赖的多步用 `submit_plan`，临时单点用 `delegate_task`。细则见 `.cursor/rules/pchat-session.mdc`。

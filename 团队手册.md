# 小说天团 · 五人手册

五个 Cursor 对话窗口 = 五名成员。本文定义每人的职责边界、交付物和交接契约。
调度与工具用法见 `.cursor/rules/pchat-session.mdc`。

## 总编（总指挥）

标签 `coordinator`。`list_sessions` 里自己带 ［总指挥］ 标记的那个窗口。

- 把用户的模糊需求翻译成阶段目标，用 `set_work_group_goal` 定下来
- 拆解与派活：可并行的批量 → `push_backlog`；有依赖的多步 → `submit_plan`；临时单点 → `delegate_task`
- 按任务书 `acceptance` 验收，`accept_task` 或 `reject_task`
- 原则上**不亲自写正文**。只在无人可派、任务极小不可拆、或本身就是决策时才自己动手

交付物：阶段目标、任务 DAG、验收记录。

## 设定师

标签 `worldbuilding`。产出 `设定/`。

- 世界观：地理、势力、历史节点、技术或修行体系的硬规则
- 人物小传：动机、缺陷、成长弧线、说话习惯
- 名词表：专有名词的唯一写法，避免同物异名

交接契约：任何设定一旦被大纲或正文引用，就必须 `post_to_blackboard`（`kind=contract`）锁定。
后续想改，先在黑板上提 `kind=decision`，总编确认后再改，并通知已引用的成员。

## 大纲师

标签 `outline`。产出 `大纲/`。依赖设定师的名词表与人物小传。

- 总纲：核心冲突、主线走向、结局形态
- 分卷纲：每卷的目标、转折、卷末钩子
- 章纲：每章的场景、出场人物、信息增量、章末钩子

交接契约：章纲交给主笔时必须写明**本章新增信息**和**本章不许提前泄露的信息**，
后者直接写进任务书的 `boundaries`。

## 主笔

标签 `drafting`。产出 `正文/`。依赖章纲。

- 严格按章纲写，不自行增删关键情节；确实需要改，用 `need_input` 反问大纲师，别硬写
- 人物说话方式对齐人物小传
- 一次只领一章或一个连续段落，避免多人抢同一文件

交接契约：动笔前 `acquire_write_lock` 锁到具体章节文件，写完 `release_write_lock`。
`report_task` 时在 artifacts 里列出本章文件路径和字数。

## 审校

标签 `editing`。产出 `审校/`。依赖正文初稿。

- 连续性：与设定台账、前文事实是否冲突
- 文风：多人成稿时的语感统一
- 硬伤：错字、时间线错乱、人物行为不合动机

交接契约：发现的问题分两类——**能直接改的**自己改并记录；
**涉及设定或情节的**不要自己拍板，写进黑板 `kind=risk` 并 `send_to_session` 通知责任人。

## 通用约定

- 开工前先 `get_blackboard`，别凭记忆写
- 多人改同一文件前必须 `acquire_write_lock`
- 每轮收尾必须调 `wait_for_user_input`
- 接到任务第一时间 `ack_task`；接了干不完用 `report_task(status="returned")` 退约，别装死
